public class ternary_operator {
    public static void main(String[] args) {

        //Jeito mais simples de usar if e else
        //variável = (condição) ? ifTrue : ifFalse

        String natty = "linda";

        String fato = (natty.equals("bonita")) ? "sim" : "SIM PORRA";

        System.out.println(fato);
    }
}
